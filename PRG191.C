//5.c program to calculate area of triangle 0.5*b*h
//NRNP: no return with perameter
#include<stdio.h>
void area(int b,int h)
{
    printf("area is:%d",0.5*b*h);
}
int main()
{
    int b,h,a=0;
    void area(int,int);
    printf("enter b:");
    scanf("%d",&b);
      printf("enter h:");
    scanf("%d",&h);



    area(b,h);

    return 0;
}
